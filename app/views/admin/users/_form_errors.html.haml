- if user.errors.any?
  .form-base.row
    .col-md-12.no-padding.form-errors
      .text-center
        .with-padding
          #error_explanation
            %h2 #{ pluralize(user.errors.count, "error") } prohibited this user from being saved:

            %ul
              - user.errors.full_messages.each do |message|
                %li= message

  %br%br
